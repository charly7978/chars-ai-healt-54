/**
 * Capa decorativa 3D desactivada: la interfaz principal es solo el monitor PPG (PPGSignalMeter).
 * Se exporta un no-op por si algún import legacy reaparece en el bundle.
 */
export function MedicalAmbient3D() {
  return null;
}
